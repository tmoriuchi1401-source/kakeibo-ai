name: Amazon legacy entry retired

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kakeibo-production
  cancel-in-progress: false
  queue: max

jobs:
  retired:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Use the unified production entry
        run: echo "Amazon order tracking is retired. Use kakeibo-production with the confirmed money migration."
